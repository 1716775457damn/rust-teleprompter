[package]
name = "rust-teleprompter"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.28"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["clock"] }
